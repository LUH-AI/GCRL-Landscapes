# Cluster profile for LUH (Leibniz University Hannover) — ai,tnt partitions
# Used by submission.py (via --cluster arg) and launcher.yaml (Hydra/SMAC HPO)

partition: "ai,tnt"
reservation: "ai,tnt"
gres: null          # null = use gpus_per_node: 1
gpus_per_node: 1
